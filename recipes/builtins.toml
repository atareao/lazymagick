
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# NEW BASIC RECIPES
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[[recipe]]
category = "Adjust"
name = "auto-orient"
description = "Auto-rotate and flip based on EXIF orientation, then strip orientation metadata"
tags = ["orient", "exif", "rotate"]
output_ext = "auto"
stages = [{ flags = ["-auto-orient", "-strip"] }]

[[recipe]]
category = "Resize"
name = "fit within"
description = "Shrink to fit within 1920×1080, only if larger (use \">\" geometry modifier)"
tags = ["resize", "scale", "fit"]
output_ext = "auto"
stages = [{ flags = ["-strip", "-resize", "1920x1080>"] }]

[[recipe]]
category = "Resize"
name = "crop center"
description = "Crop a 800×800 square from the center of the image"
tags = ["crop", "cut", "square"]
output_ext = "auto"
stages = [{ flags = ["-gravity", "center", "-crop", "800x800+0+0", "+repage"] }]

[[recipe]]
category = "Convert"
name = "convert"
description = "Pure format conversion without any transformations — just change the container"
tags = ["convert", "format"]
output_ext = "auto"
stages = [{ flags = [] }]

[recipe.formats]
jpeg = ["-quality", "92"]
png = []
webp = ["-quality", "85"]
avif = ["-quality", "60"]
jxl = ["-quality", "85"]

[[recipe]]
category = "Adjust"
name = "normalize"
description = "Auto-level colours: stretch contrast to full range, remove colour cast"
tags = ["color", "contrast", "auto"]
output_ext = "auto"
stages = [{ flags = ["-normalize"] }]

[[recipe]]
category = "Optimize"
name = "compress"
description = "Aggressive web-optimised compression: strip metadata + quality 80 with chroma subsampling"
tags = ["compress", "optimize", "web"]
output_ext = "auto"
stages = [{ flags = ["-strip"] }]

[recipe.formats]
jpeg = ["-quality", "80", "-sampling-factor", "4:2:0"]
webp = ["-quality", "80"]
avif = ["-quality", "50", "-effort", "6"]
png = ["-quality", "80"]
jxl = ["-quality", "75"]

[[recipe]]
category = "Optimize"
name = "reduce palette"
description = "Reduce to 256 colours with 8-bit depth — creates indexed PNG"
tags = ["palette", "colors", "indexed", "gif"]
output_ext = "png"
stages = [{ flags = ["-colors", "256", "-depth", "8"] }]

[recipe.formats]
png = ["-colors", "256", "-depth", "8"]
gif = ["-colors", "256"]

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# NEW ADVANCED / EFFECT RECIPES
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[[recipe]]
category = "Filter"
name = "sharpen"
description = "Unsharp mask sharpening (radius 0, sigma 1.0)"
tags = ["sharpen", "unsharp", "detail"]
output_ext = "auto"
stages = [{ flags = ["-sharpen", "0x1.0"] }]

[[recipe]]
category = "Filter"
name = "blur"
description = "Gaussian blur with sigma 3 — softens the image"
tags = ["blur", "gaussian", "soften"]
output_ext = "auto"
stages = [{ flags = ["-blur", "0x3"] }]

[[recipe]]
category = "Decor"
name = "border"
description = "Add a 10px solid black border around the image"
tags = ["border", "frame", "outline"]
output_ext = "auto"
stages = [{ flags = ["-bordercolor", "black", "-border", "10x10"] }]

[[recipe]]
category = "Decor"
name = "shadow"
description = "Add a drop shadow effect (opacity 80%, offset 5+5, blur radius 5)"
tags = ["shadow", "effect", "3d"]
output_ext = "auto"
stages = [{ flags = ["-background", "black", "-shadow", "80x5+5+5", "+repage"] }]

[[recipe]]
category = "Decor"
name = "watermark"
description = "Overlay a text watermark at the bottom-right corner"
tags = ["watermark", "text", "label"]
output_ext = "auto"
stages = [{ flags = ["-font", "Helvetica", "-pointsize", "48", "-fill", "white", "-gravity", "southeast", "-annotate", "+20+20", "© 2025"] }]

[[recipe]]
category = "Color"
name = "sepia"
description = "Apply a sepia tone effect for a vintage/retro look"
tags = ["sepia", "vintage", "retro", "tone"]
output_ext = "auto"
stages = [{ flags = ["-sepia-tone", "80%"] }]

[[recipe]]
category = "Filter"
name = "sketch"
description = "Pencil sketch / drawing effect (radius 0, sigma 10, angle 120°)"
tags = ["sketch", "drawing", "pencil", "art"]
output_ext = "auto"
stages = [{ flags = ["-sketch", "0x10+120"] }]

[[recipe]]
category = "Optimize"
name = "trim"
description = "Auto-crop solid-colour borders from the edges of the image"
tags = ["trim", "crop", "border", "auto"]
output_ext = "auto"
stages = [{ flags = ["-trim", "+repage"] }]

[[recipe]]
category = "Convert"
name = "avif 50"
description = "Convert to AVIF with quality 50, strip metadata"
tags = ["avif", "convert", "lossy"]
output_ext = "avif"
stages = [{ flags = ["-strip"] }]

[recipe.formats]
avif = ["-quality", "50", "-effort", "5"]
webp = ["-quality", "85"]
jpeg = ["-quality", "90"]

[[recipe]]
category = "Color"
name = "grayscale"
description = "Convert to grayscale"
tags = ["color", "grayscale"]
output_ext = "auto"
stages = [{ flags = ["-colorspace", "Gray"] }]

[[recipe]]
category = "Convert"
name = "jpeg 75"
description = "Convert to JPEG with quality 75"
tags = ["jpeg", "convert", "lossy"]
output_ext = "jpg"
stages = [{ flags = ["-strip", "-interlace", "Plane", "-quality", "75"] }]

[recipe.formats]
webp = ["-quality", "75"]
png = ["-quality", "75"]

[[recipe]]
category = "Convert"
name = "jpeg 90"
description = "High-quality JPEG conversion with interlacing"
tags = ["jpeg", "convert", "lossy"]
output_ext = "jpg"
stages = [{ flags = ["-strip", "-interlace", "Plane", "-quality", "90"] }]

[recipe.formats]
png = []
webp = ["-quality", "90"]

[[recipe]]
category = "Convert"
name = "jpeg xl"
description = "Convert to JPEG XL — next-gen compression"
tags = ["jxl", "convert", "lossy"]
output_ext = "jxl"
stages = [{ flags = ["-strip"] }]

[recipe.formats]
jxl = ["-quality", "80", "-effort", "5"]
webp = ["-quality", "85"]
png = []

[[recipe]]
category = "Optimize"
name = "png optimize"
description = "Optimize PNG with stripping and filtering"
tags = ["png", "optimize"]
output_ext = "png"
stages = [{ flags = ["-strip"] }]

[recipe.formats]
png = ["-quality", "95"]

[[recipe]]
category = "Resize"
name = "resize 1920w"
description = "Resize to 1920px wide, maintain aspect ratio"
tags = ["resize", "scale"]
output_ext = "auto"
stages = [{ flags = ["-resize", "1920x"] }]

[[recipe]]
category = "Resize"
name = "resize 50%"
description = "Scale to 50% of original dimensions"
tags = ["resize", "scale"]
output_ext = "auto"
stages = [{ flags = ["-resize", "50%"] }]

[[recipe]]
category = "Optimize"
name = "strip"
description = "Strip all metadata and profiles"
tags = ["strip", "metadata", "clean"]
output_ext = "auto"
stages = [{ flags = ["-strip"] }]

[[recipe]]
category = "Resize"
name = "thumbnail"
description = "Generate a 300x300 thumbnail with adaptive resize"
tags = ["thumbnail", "resize"]
output_ext = "auto"
stages = [{ flags = ["-thumbnail", "300x300^", "-gravity", "center", "-extent", "300x300"] }]

[[recipe]]
category = "Convert"
name = "webp 85"
description = "Convert to WebP with quality 85"
tags = ["webp", "convert", "lossy"]
output_ext = "webp"
stages = [{ flags = ["-strip"] }]

[recipe.formats]
webp = ["-quality", "85"]
avif = ["-quality", "50"]

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# INSTAGRAM-STYLE FILTERS
# Approximaciones de los filtros clásicos de Instagram
# usando operadores nativos de ImageMagick (-modulate,
# -colorize, -sigmoidal-contrast, -vignette, etc.)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[[recipe]]
category = "Filter"
name = "clarendon"
description = "Instagram Clarendon: boosted contrast, cool shadows, vibrant midtones — el filtro más icónico"
tags = ["instagram", "clarendon", "contrast", "cool"]
output_ext = "auto"
stages = [{ flags = ["-sigmoidal-contrast", "6,50%", "-modulate", "100,90,105", "-auto-level"] }]

[[recipe]]
category = "Filter"
name = "lo-fi"
description = "Instagram Lo-Fi: alto contraste, saturado, con viñeta oscura — estilo retro intenso"
tags = ["instagram", "lofi", "contrast", "vignette"]
output_ext = "auto"
stages = [{ flags = ["-sigmoidal-contrast", "8,50%", "-vignette", "28x20+15+5"] }]

[[recipe]]
category = "Filter"
name = "valencia"
description = "Instagram Valencia: cálido, desvaído, toque dorado — aspecto vintage suave"
tags = ["instagram", "valencia", "warm", "faded"]
output_ext = "auto"
stages = [{ flags = ["-modulate", "110,70,100", "-fill", "#d4a017", "-colorize", "5%", "-sigmoidal-contrast", "3,50%"] }]

[[recipe]]
category = "Filter"
name = "xpro ii"
description = "Instagram X-Pro II: alto contraste, dominante naranja, viñeta fuerte — look analógico cross-processed"
tags = ["instagram", "xpro", "contrast", "warm", "vignette"]
output_ext = "auto"
stages = [{ flags = ["-sigmoidal-contrast", "12,50%", "-fill", "#e07000", "-colorize", "8%", "-vignette", "35x25+20+10"] }]

[[recipe]]
category = "Filter"
name = "nashville"
description = "Instagram Nashville: cálido con tinte rosado suave y realce de sombras — look pastel"
tags = ["instagram", "nashville", "warm", "pink", "pastel"]
output_ext = "auto"
stages = [{ flags = ["-modulate", "115,50,105", "-fill", "#ffb3ba", "-colorize", "8%", "-level", "5%,95%"] }]

[[recipe]]
category = "Filter"
name = "walden"
description = "Instagram Walden: frío, desaturado, tinte azul pálido — aspecto sereno y etéreo"
tags = ["instagram", "walden", "cool", "muted", "blue"]
output_ext = "auto"
stages = [{ flags = ["-modulate", "100,50,110", "-fill", "#87ceeb", "-colorize", "5%"] }]

[[recipe]]
category = "Filter"
name = "toaster"
description = "Instagram Toaster: centro iluminado, bordes oscuros, destellos naranja-cobre"
tags = ["instagram", "toaster", "warm", "vignette", "copper"]
output_ext = "auto"
stages = [{ flags = ["-fill", "#cc5500", "-colorize", "10%", "-vignette", "20x15+5+5", "-modulate", "105,90,100"] }]

[[recipe]]
category = "Filter"
name = "earlybird"
description = "Instagram Earlybird: vintage, sepia suave, bordes oscuros — foto de los años 70"
tags = ["instagram", "earlybird", "vintage", "warm", "sepia"]
output_ext = "auto"
stages = [{ flags = ["-fill", "#8b4513", "-colorize", "8%", "-sepia-tone", "70%", "-vignette", "30x25+15+10"] }]

[[recipe]]
category = "Filter"
name = "gingham"
description = "Instagram Gingham: lavado, baja saturación, tinte azul frío en sombras — look desgastado"
tags = ["instagram", "gingham", "muted", "cool", "faded"]
output_ext = "auto"
stages = [{ flags = ["-modulate", "100,60,105", "-fill", "#b0c4de", "-colorize", "5%", "-brightness-contrast", "-5,-10"] }]

[[recipe]]
category = "Filter"
name = "willow"
description = "Instagram Willow: monocromático con tinte púrpura, alto contraste — elegante y dramático"
tags = ["instagram", "willow", "mono", "purple", "dramatic"]
output_ext = "auto"
stages = [{ flags = ["-colorspace", "Gray", "-fill", "#4b0082", "-colorize", "15%", "-sigmoidal-contrast", "8,50%"] }]

[[recipe]]
category = "Filter"
name = "amaro"
description = "Instagram Amaro: dorado suave, centro más brillante, bordes ligeramente más oscuros"
tags = ["instagram", "amaro", "warm", "soft", "golden"]
output_ext = "auto"
stages = [{ flags = ["-modulate", "105,85,100", "-fill", "#ffd700", "-colorize", "3%", "-vignette", "25x20+10+5"] }]

[[recipe]]
category = "Filter"
name = "inkwell"
description = "Instagram Inkwell: blanco y negro puro con alto contraste — clásico atemporal"
tags = ["instagram", "inkwell", "bw", "mono", "contrast"]
output_ext = "auto"
stages = [{ flags = ["-colorspace", "Gray", "-sigmoidal-contrast", "10,50%"] }]

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# WEIGHT REDUCTION — COMPRESSION LEVELS
# De menos a más agresiva: ligera (casi sin pérdida),
# media (buena compresión), agresiva (máxima reducción)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[[recipe]]
category = "Compress"
name = "weight light"
description = "Compresión ligera: strip metadata, JPEG calidad 92 — casi sin pérdida visible"
tags = ["compress", "light", "web", "strip"]
output_ext = "auto"
stages = [{ flags = ["-strip"] }]

[recipe.formats]
jpeg = ["-quality", "92"]
webp = ["-quality", "92"]
avif = ["-quality", "80"]
png = ["-quality", "92"]
jxl = ["-quality", "92"]

[[recipe]]
category = "Compress"
name = "weight medium"
description = "Compresión media: strip, JPEG calidad 75, chroma 4:2:0 — ~50-70% de reducción"
tags = ["compress", "medium", "web", "strip"]
output_ext = "auto"
stages = [{ flags = ["-strip"] }]

[recipe.formats]
jpeg = ["-quality", "75", "-sampling-factor", "4:2:0"]
webp = ["-quality", "75"]
avif = ["-quality", "60", "-effort", "4"]
png = ["-quality", "75"]
jxl = ["-quality", "70"]

[[recipe]]
category = "Compress"
name = "weight aggressive"
description = "Compresión agresiva: strip, JPEG calidad 50, chroma 4:2:0, paleta 256 colores, resize 1920px — máxima reducción"
tags = ["compress", "aggressive", "web", "strip", "palette"]
output_ext = "auto"
stages = [{ flags = ["-strip", "-resize", "1920x>", "-colors", "256"] }]

[recipe.formats]
jpeg = ["-quality", "50", "-sampling-factor", "4:2:0"]
webp = ["-quality", "50"]
avif = ["-quality", "30", "-effort", "6"]
png = ["-quality", "50"]
jxl = ["-quality", "50"]

[[recipe]]
category = "Convert"
name = "png to jpg"
description = "PNG → JPG con paleta 256 colores y calidad 75 — tu workflow actual para reducir fotos"
tags = ["png", "jpg", "convert", "palette", "256"]
output_ext = "jpg"
stages = [{ flags = ["-strip", "-colors", "256", "-quality", "75"] }]